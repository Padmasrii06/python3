class Solution:
    def isPerfectSquare(self, num: int) -> bool:
        st=1
        end=num
        while(st<=end):
            mid=(st+end)//2
            if mid*mid==num:
                return True
            elif mid*mid<num:
                st=mid+1
            else:
                end=mid-1
        return False
