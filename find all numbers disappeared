class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:
        newList =[]
        for i in range(1,len(nums)+1):
            newList.append(i)
        
        difference = set(newList) - set(nums)
        return(list(difference))
